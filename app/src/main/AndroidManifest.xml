<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="leesimjeonsim.user.cosmeticlifecycle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".GetInformActivity"
            android:label="@string/title_activity_get_inform" />
        <activity
            android:name=".MainList_Activity"
            android:label="@string/title_activity_main_list_" />
        <activity android:name=".CsmtAddActivity" />
        <activity android:name=".JoinActivity" />
        <activity
            android:name=".RcmdimfActivity">
        </activity>
    </application>

</manifest>